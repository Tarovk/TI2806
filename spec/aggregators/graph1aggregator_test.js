define([], function (aggregator) {
    describe('Test suite for the aggregator of graph 1', function () {
        it('should aggregate data', function () {
            var username = "lvdoorn";
            //var aggregator = new Graph1Aggregator(username);
            //console.log(setSemanticEvents(undefined));
        });
    });
});
